{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the local configuration manager",
  "windows": ["main"],
  "permissions": ["core:default"]
}
